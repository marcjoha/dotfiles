syntax enable
set background=dark

" Enable 24-bit TrueColor support for modern terminals (like Ghostty)
if has('termguicolors')
  set termguicolors
endif

" The original 2011 altercation/vim-colors-solarized is not compatible with termguicolors.
" To solve this perfectly on Ghostty, we've bundled the modern "Solarized 8" theme (colors/solarized8.vim)
" which fully supports true color, and falls back gracefully to the original theme in 256-color mode if needed.
let g:solarized_termtrans = 1 " This gets rid of the grey background
silent! colorscheme solarized8

if !exists('g:colors_name') || g:colors_name != 'solarized8'
  " Fallback to classic solarized in 256-color mode if solarized8 is unavailable
  let g:solarized_termcolors=256
  silent! colorscheme solarized
endif

" Tab and indentation settings
set tabstop=2             " number of visual spaces per TAB
set softtabstop=2         " number of spaces in tab when editing
set expandtab             " tabs are spaces
filetype indent on        " load filetype-specific indent files

" Display settings
set number                " show line numbers
set showcmd               " show command in bottom bar
set wildmenu              " visual autocomplete for command menu
set lazyredraw            " redraw only when we need to.
set showmatch             " highlight matching [{()}]

" Search settings
set incsearch             " search as characters are entered
set nohlsearch            " don't highlight matches

" Interface and editor behavior
set mouse=a               " allows for highlighting text with mouse
set clipboard=unnamedplus " copy Vim's yank-stuff into system clipboard

" Fix backspace keys so it behaves intuitively in insert mode
set backspace=indent,eol,start

" Disable annoying local swap/backup files in project directories
set nobackup
set nowritebackup
set noswapfile
